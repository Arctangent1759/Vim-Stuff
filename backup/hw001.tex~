\documentclass[11pt]{article}
\usepackage{amsmath,textcomp,amssymb,geometry,graphicx,enumerate}
\def\Name{Alexander Chu}  % Your name
\def\Sec{111}  % Your discussion section
\def\Login{cs70-kd} % Your login
\def\HW{1} % Homework number


\title{CS70--Spring 2013}
\author{\Name, section \Sec, \texttt{\Login}}
\markboth{CS70--Spring 2013 Homework \HW, \Name, section \Sec}{CS70--Spring 2013 Homework \HW, \Name, section \Sec, \texttt{\Login}}
\pagestyle{myheadings}

\begin{document}

\maketitle

\textbf{Collaborators: Robert Chang, Niraj Amalkanti} % Enter your collaborators

\begin{enumerate}[1.)]
	\item $\ln 2$
	\item Ask which door the other brother thinks has the treasure. Then, choose the other door. \\
		If the treasure is behind the lying brother's door, the true brother would say that the lying brother would say that the treasure is behind the true brother's door. The lying brother would say that the true brother would say that the treasure is behind the true brother's door. \\
		If the treasure is behind the true brother's door, the true brother would say that the lying brother would say that the treasure is behind the lying brother's door. The lying brother would say that the true brother would say that the treasure is behind the lying brother's door. \\
		Hence, in both cases, both brothers would indicate the room with the angry goats, so choose the other door.\\
	\item Truth Table:\\
		\begin{tabular}{c | c || c || c || c || c}
			$P$ & $Q$ & $P \oplus Q$ &  $(P \wedge \neg Q)$ & $(\neg P \wedge Q)$ & $(P \wedge \neg Q) \vee (\neg P \wedge Q)$\\
			\hline
			T&T&F&F&F&F\\
			T&F&T&T&F&T\\
			F&T&T&F&T&T\\
			F&F&F&T&F&F\\
		\end{tabular}\\
		$ \Rightarrow P \oplus Q \equiv (P \wedge \neg Q) \vee (\neg P \wedge Q) $
	
	\item
		\begin{enumerate}[1.]
			\item True. Let $P=\mbox{30 is divisible by 10}$. Let $Q=\mbox{40 is divisible by 10}$. $P$ is true and $Q$ is true. Therefore, $P \Rightarrow Q$ is true. 
			\item True. Let $P=\mbox{30 is divisible by 9}$. $P$ is false. Therefore, $P \Rightarrow Q$ is true. 
			\item False. Let $P=\mbox{30 is divisible by 10}$. Let $Q=\mbox{40 is divisible by 9}$. $P$ is true and $Q$ is false. Therefore, $P \Rightarrow Q$ is false. 
			\item True. Let $P=\mbox{30 is divisible by 9}$. $P$ is false. Therefore, $P \Rightarrow Q$ is true. 
		\end{enumerate}
	\item $S$ does not contain 10, 11, and 12. Rule 2 states that $x \in S \Rightarrow 2x \in S$. The contrapositive of this rule states that $\neg (2x \in S) \Rightarrow \neg (x \in S)$. Therefore, 3, 5, and 6 are not in $S$. Rule 1 states $x \in S \Rightarrow -x \in S$. The contrapositive of this rule states that $\neg (-x \in S) \Rightarrow \neg (x \in S)$. Therefore, -3, -6, -5, -10, -11, and -12 are not in $S$. \\
	Therefore, S is a set that does not contain -12, -11, -10, -6, -5, -3, 3, 5, 6, 10, 11, and 12.
	\item 
		\begin{enumerate}[1.]
			\item True.\\
				$(\forall x \in \mathbb{N} x^2<5) \Rightarrow (\forall x \in \mathbb{N} x^2<4)$\\
				$F \Rightarrow F$\\
				$T$
			\item True.\\
				$(\forall x \in \mathbb{N} x^2<4) \Rightarrow (\forall x \in \mathbb{N} x^2<5)$\\
				$F \Rightarrow F$\\
				$T$
			\item False.\\
				$\forall x \in \mathbb{N} (x^2<5 \Rightarrow \forall x \in \mathbb{N} x^2<4)$\\
				$F$
			\item True.\\
				$\forall x \in \mathbb{N} (x^2<4 \Rightarrow \forall x \in \mathbb{N} x^2<5)$\\
				$T$
			\item True.\\
				$Q(n) \Rightarrow 1 + 2 + \cdots + n = \frac{n(n+1)}{2}$\\
				$Q(n) \Rightarrow 2 + 4 + \cdots + 2n = n^2+n$\\
				$Q(n+1) \Rightarrow  1 + 2 + \cdots + n + (n + 1) = \frac{(n+1)(n+2)}{2}$\\
				$Q(n+1) \Rightarrow  2 + 4 + \cdots + 2n + (2n + 2) = (n^2+3n+2)$\\
				$Q(n+1) \Rightarrow  2 + 4 + \cdots + 2n = n^2+n$\\
				$Q(n) \Rightarrow  Q(n+1)$\\
				$T$
		\end{enumerate}
	\item 
		\begin{enumerate}[1.]
			\item P(n) is true for 0, 1, and the prime numbers. The possible values of $i$ encompass all numbers from $0$ to $n$. The condition requires either for $j$ to be equal to $n$, or for $ij$ to not equal n. These are clearly the conditions for prime numbers, sans the condition that $i \neq j$.
			\item $\exists i \in \mathbb{N}. \exists j \in \mathbb{N}. \exists k \in \mathbb{N}. \exists \ell \in \mathbb{N}. f(i,j)=g(k,\ell). $
		\end{enumerate}
	\item 
		\begin{enumerate}[1.]
			\item \textbf{Direct Proof:}\\
				\textbf{Step 1:} Prove by contraposition that $n^2$ is even $\Rightarrow n$ is even.\\
				Suppose $n$ is odd.\\
				$\Rightarrow \exists k \in \mathbb{N}, 2k+1=n$\\
				$\Rightarrow n^2 = (2k+1)^2 = 4k^2+4k+1 = 2(2k^2+2k)+1$\\
				$\Rightarrow \exists j \in \mathbb{N}, j=2k^2+2k$\\
				$\Rightarrow n^2 = 2j+1$\\
				$\Rightarrow n^2$ is odd\\
				By contraposition, $n^2$ is even $\Rightarrow n$ is even.\\
				\textbf{Step 2:} Direct proof that $n$ is even $\Rightarrow n^5$ is even.\\
				Assume n is even.\\
				$\Rightarrow \exists k \in \mathbb{N}, 2k=n$\\
				$\Rightarrow n^5=(2k)^5=2^5k^5=2(2^4k^5)$\\
				$\Rightarrow \exists j \in \mathbb{N}, j=2^4k^5$\\
				$\Rightarrow n^5=2j$\\
				$\therefore n$ is even $\Rightarrow n^5$ is even.\\
				But $n^2$ is even  $\Rightarrow n$ is even.\\
				$\therefore n^2$ is even $\Rightarrow n^5$ is even.\\
				\textsc{qed}
			\item \textbf{Direct Proof:}\\
				Suppose n is even\\
				Let $n=h,h \in \mathbb{N}$ be an arbitrary even number.\\
				$\Rightarrow \forall h, \exists k \in \mathbb{N}, h=2k$
				$\Rightarrow h^2-h+3=(2k)^2-(2k)+3=4k^2-2k+(2+1)=2(2k^2-k+1)+1$
				$\Rightarrow \exists \ell \in \mathbb{N}, \ell=2k^2-k+1$
				$\Rightarrow h^2-h+3=2\ell +1$\\
				$\Rightarrow n^2-n+3$ is odd.\\
				Suppose n is odd\\
				Let $n=i,i \in \mathbb{N}$ be an arbitrary odd number.\\
				$\Rightarrow \forall i, \exists j \in \mathbb{N}, i=2j+1$
				$\Rightarrow i^2-i+3=(2j+1)^2-(2j+1)+3=4j^2+4j+1-2j-1+3=4j^2+2j+(2+1)=2(2j^2+j+1)+1$
				$\Rightarrow \exists \ell \in \mathbb{N}, \ell=2j^2+j+1$
				$\Rightarrow i^2-i+3=2\ell +1$\\
				$\Rightarrow n^2-n+3$ is odd.\\
				$\therefore n$ is odd $\vee$ $n$ is even $\Rightarrow n^2-n+3$ is odd.\\
				$n^2-n+3$ is odd $\forall n \in \mathbb{N}$ 
				\textsc{qed}
			\item \textbf{Proof by Case}\\
				Suppose that $x<10 \wedge y<10$\\
				$\Rightarrow x+y<20$\\
				By contraposition, $x+y \ge 20 \Rightarrow x \ge 10 \vee y \ge 10$\\
				\textsc{qed}
			\item \textbf{Disproof by Counterexample}\\
				Let $r=\sqrt(1759)$\\
				$r$ is irrational\\
				$r^2=1759$ is rational\\
				Counterexample reached.\\
				\textsc{qed}
		\end{enumerate}
	$ $\\
	\item F. The approach the student uses is wrong, because he attempts to prove the converse of the statement. He begins with the premise $Q=(n+1)^2 \le n+1$. Also, the student makes an alegbraic error on line 5. While moving the $2n$ term to the right side, the student combines the $n$ and $-2n$ terms incorrectly. The expression after combination should be $n^2 \le -n$. The student's error results in the fallacious conclusion that $n^2 \le n \Rightarrow (n+1)^2 \le n+1$. (The claim is wrong because $n^2 \le n$ is true for $n \in \{0,1\}$, and $(1+1)^2 \le 1+1$ is a false statement.)
\end{enumerate}
\end{document}
