from django.db import models

# Create your models here.

class Cat(models.Model):
  name=models.CharField(max_length=30);
  sound=models.CharField(max_length=50);
  birthday=models.DateTimeField("Cat birthday");
