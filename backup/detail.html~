{% extends "Base/twoPanedBase.html" %}
{% block content %}
		<h1>{{ newsItem.title }}</h1>
		<hr/>
		<br/>
		<img src="{{ newsItem.imageURL }}" width="40%"></img>
		<br/>
		<br/>
		<br/>
		By <strong>{{ newsItem.author }}</strong> on <em>{{ newsItem.pub_date }}</em>
		<br/>
		<br/>
		<div style="white-space: pre-wrap;">{{ newsItem.article }}</div>
		<br/>
		<br/>
		<a class='btn' href="/news" style="float:right">Back</a>
{% endblock %}

{% block sidebar %}
	<!--Page Sidebar-->
	<h2>Related Stories</h2>
	<hr/>
		<table>
			{% for story in related_stories %}
				<tr>
					<td>
						<div class="shaded">
							<a href="{% url News:detail story.id %}"><strong>{{ story.title }}</strong><br/></a>
							<div style="margin:10px">
								<a href="{% url News:detail story.id %}"><img src="{{story.imageURL}}" style="width:50%;height:50%;float:left;margin:3px;"/></a>
								{{ story.preview }}
								<a href="{% url News:detail story.id %}">... (read more)<br/></a>
							</div>
							<br/>
						</div>
					</td>
				</tr>
			{% endfor %}
		</table>

{% endblock %}
