\documentclass[11pt]{article}
\usepackage{enumerate,amsmath,textcomp,amssymb,geometry,graphicx}

\def\Name{Alexander Chu}  % Your name
\def\Sec{111}  % Your discussion section
\def\Login{cs70-kd} % Your login
\def\HW{2} % Homework number

\newcount\colveccount
\newcommand*\colvec[1]{
\global\colveccount#1
\begin{pmatrix}
\colvecnext
}
\def\colvecnext#1{
#1
\global\advance\colveccount-1
\ifnum\colveccount>0
\\
\expandafter\colvecnext
\else
\end{pmatrix}
\fi
}

\title{CS70--Spring 2013}
\author{\Name, section \Sec, \texttt{\Login}}
\markboth{CS70--Spring 2013 Homework \HW, \Name, section \Sec}{CS70--Spring 2013 Homework \HW, \Name, section \Sec, \texttt{\Login}}
\pagestyle{myheadings}

\begin{document}
\maketitle

\textbf{Collaborators: Robert Chang, Niraj Amalkanti, Fang Cai} % Enter your collaborators

\begin{enumerate}[1)]
% Have a new \item for each problem
% Make sure you have a \newpage after each item
	\item \textbf{Base Case:}$s_2=1-\frac{1}{2}=\frac{1}{2}$\\
		\textbf{Inductive Hypothesis:}Assume $s_n=(1-\frac{1}{2})\times(1-\frac{1}{3})\times \cdots \times (1-\frac{1}{n})=\frac{1}{n}$\\
		\textbf{Inductive Step:}$s_{n+1}=(1-\frac{1}{2})\times(1-\frac{1}{3})\times \cdots \times (1-\frac{1}{n})\times (1-\frac{1}{n+1})$\\
		$=(\frac{1}{n})(1-\frac{1}{n+1})=\frac{1}{n}-\frac{1}{n(n+1)}=\frac{n+1}{n(n+1)}-\frac{1}{n(n+1)}$\\
		$=\frac{n}{n(n+1)}$\\
		$=\frac{1}{n+1}$\\
		By induction, $s_n=\frac{1}{n}$\\
		\textsc{qed}
	\newpage
	\item Base Case: $a_0=9+4=13, \exists j \in \mathbb{N}. 13j=13$.\\
		Inductive Hypothesis: Assume $a_n=3^{n+2}+4^{2n+1}, \exists i \in \mathbb{N}. 13i=a_n$\\
		$a_{n+1}=(3^{(n+1)+2})+(16^{2(n+1)+1})$\\
		$a_{n+1}=(3)(9)(3^n)+(16)(4)(16^n)$\\
		$a_{n+1} - 3a_n = (13)(4)(16^n)$\\
		$a_{n+1} = (13)(4)(16^n) + 3a_n$\\
		$a_{n+1} = (13)(4)(16^n) + (13)(3)i$\\
		$a_{n+1} = (13)((4)(16^n) + (3)i)$\\
		$\therefore a_{n+1}$ is divisible by 13.\\
		By induction, $a_n=3^{n+2}+4^{2n+1}, a_n$ is divisible by 13.\\
		\textsc{qed}
	\newpage
	\item
		\textbf{Base Case:} A tower of size 1 can be solved in 1 move by moving it to the desired peg.\\
		\textbf{Inductive Hypothesis:} Assume that a tower of size n can be solved with $f(n)$ moves.\\
		\textbf{Inductive Step:} A tower of size $n+1$ can be solved by moving the tower of size $n$ above it to the other peg in $f(n)$ steps, moving the bottom plate from the current peg to the desired peg in $1$ step, and then moving the rest of the tower to the desired peg in $f(n)$ steps. In other words, $f(n+1)=2f(n)+1$.\\
		QED.\\
		To find a nonrecursive, form for this solution:\\
		$f(n+1)=2f(n)+1$\\
		$f(n+1)=2(f(n-1)+1)+1$\\
		$f(n+1)=2((2f(n-2)+1)+1)+1$\\
		$\cdots$\\
		$f(n+1)=2((2(\cdots f(0)+1\cdots )+1)+1)+1$\\
		$f(n+1)=\displaystyle\sum_{k=0}^{n} 2^k$\\
		$f(n)=\displaystyle\sum_{k=0}^{n-1} 2^k$\\
		Number of seconds in a century is:\\ 
		$60\frac{seconds}{minute}\times60\frac{minutes}{hour}\times24\frac{hours}{day}\times365\frac{days}{year}\times100\frac{years}{century}=3153600000$ seconds\\
		It would take $\frac{\sum_{k=0}^{n-1} 2^k}{3153600000}$ centuries for the priests to destroy the world.\\
		For n=64, it would take $5.8\times10^9$ centuries.
	\newpage
	\item
		Yes.\\
		\textbf{Base Case:} Fry can solve a stack of size 1 (the stack is already sorted).\\
		\textbf{Inductive Hypothesis:} Fry can solve a stack of size n.\\
		\textbf{Inductive Step:} Fry can move the largest pizza of a stack of size n+1 to the bottom of the stack of size n+1 by flipping the stack from right below the largest, and then flipping the entire stack.\\
		On top of the largest pizza now at the bottom of the stack rests a stack of size n, which, according to our inductive hypothesis, can be solved.\\
		By induction, all stacks can be solved.
		
	\newpage
	\item
		Let $v(r,y,b)=13y+4r+b$ represent the value of $r$ red birds, $y$ yellow birds, and $b$ black birds.\\
		Let $v_n$ represent the value of the birds at turn n.\\
		We will prove that $v$ decreases to our base cases by strong induction.\\
		\textbf{Base Case:} 
		\begin{enumerate}[1.]
			\item
				For $y=0$, $r=0$, $r=0$, $v(r,y,b)=v_0=0$, and the game terminates because there are no more valid moves.\\
				$\therefore v=0 \implies termination$
			\item
				For $y=0$, $r=1$, $r=0$, $v(r,y,b)=v_0=4$, and the game terminates because there are no more valid moves.\\
				$\therefore v=4 \implies termination$
		\end{enumerate}
		\textbf{Inductive Hypothesis:} Assume $v_{i}=v_{i-1}-1 \forall 0\le i\le n$.\\
		\textbf{Inductive Step:} 
		Proof by cases:\\
		\begin{enumerate}[1.]
			\item
				On turn $n+1$, if the player chooses action \textbf{i}, to shoot 1 yellow bird and add 3 red birds, $v$ decreases by $1\times13=13$ and increases by $3*4=12$.\\
				$-13+12=-1$.\\
				Hence, $v_{n+1}=v_n-1$.\\
			\item
				On turn $n+1$, if the player chooses action \textbf{ii}, to shoot 2 red birds and add 7 black birds, $v$ decreases by $2\times4=8$ and increases by $7*1=7$.\\
				$-8+7=-1$.\\
				Hence, $v_{n+1}=v_n-1$.\\
			\item
				On turn $n+1$, if the player chooses action \textbf{iii}, to shoot 2 red birds and add 7 black birds, $v$ decreases by $1\times1=1$.\\
				Hence, $v_{n+1}=v_n-1$.\\
		\end{enumerate}
		By strong induction, $v$ will decrease for each turn after n. Because $v$ is bounded by $v=0$ or $v=4$, for which there are no more valid steps, for finite $y,r,b$, the game will always reach termination.

	\newpage
	\item
		\begin{enumerate}[1.]
			\item F. The student's base case is incorrect. For $n=1, n^2+3n=1+3=4$ is even.
			\item A.
			\item A.
			\item F. The statement $max(x,y)=k+1\implies max(x-1,y-1)=k$ is not true $\forall x \in \mathbb{N}$ (for $x=0$ or $y=0$).
		\end{enumerate}
	\newpage

\end{enumerate}
\end{document}
