<table>
    <tr><th>Title</th><th>Text</th></tr>
    <% @posts.each do |p| %>
        <tr>
            <td><%= p.title %></td>
            <td><%= p.text %></td>
            <td><%= link_to 'show', post_path(p)  %></td>
            <td><%= link_to 'edit', edit_post_path(p) %></td>
            <td><%= link_to 'delete', post_path(p), method: :delete, data:{confirm: 'Are you sure?'} %></td>
        </tr>
    <% end %>
</table>
<%= link_to 'New Post', new_post_path %>
