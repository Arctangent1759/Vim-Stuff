package graph;
import DList.DList;

/**
 *
 * A DList specifically for vertices
 * Removes the need to typecast every time you get an item from a list
 *
 **/
class VertexList extends DList<Vertex>{
  
}
